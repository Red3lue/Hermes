#!/usr/bin/env node
// @hermes/cli — out of scope for v2. v1 demo lives in examples/agents.
// v2 biome operations are demoed via examples/biome-demo and apps/web.
// Stub only so the workspace build stays green.
console.log("hermes CLI: not implemented in v2 — see examples/biome-demo and apps/web.");
